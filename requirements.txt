# ЗАВИСИМОСТИ BOTHOST + ПОДДЕРЖКА ПОЛЬЗОВАТЕЛЬСКИХ БОТОВ
# Эти библиотеки нужны для работы хоста и запуска ботов пользователей

# === Telegram (для главного бота) ===
aiogram>=3.0
telethon
cryptg
aiohttp

# === Telegram (для пользовательских ботов) ===
python-telegram-bot
pyTelegramBotAPI
pyrogram
TgCrypto

# === AI (для пользовательских ботов) ===
openai

# === HTTP клиенты ===
requests
httpx

# === Утилиты ===
python-dotenv

# === Асинхронность ===
asyncio-mqtt
aiofiles

# ============================================
# ⚠️ НЕ ДОБАВЛЯТЬ сюда FastAPI, Flask, Django, uvicorn!
# Railway увидит их и попытается запустить как веб-приложение
# 
# Если пользователь хочет FastAPI в своём боте —
# он должен установить его через subprocess в коде:
# subprocess.check_call([sys.executable, "-m", "pip", "install", "fastapi", "uvicorn"])
# ============================================
