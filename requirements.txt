# ЗАВИСИМОСТИ САМОГО BOTHOST (не пользовательских ботов!)
# Эти библиотеки нужны для работы хоста

# === Telegram (для главного бота) ===
aiogram>=3.0
telethon
cryptg
aiohttp

# === AI (для примеров и пользовательских ботов) ===
openai

# === HTTP клиенты ===
requests
httpx

# === Утилиты ===
python-dotenv

# ============================================
# ⚠️ НЕ ДОБАВЛЯТЬ сюда FastAPI, Flask, Django!
# Railway увидит их и попытается запустить как веб-приложение
# 
# Если пользователь хочет FastAPI в своём боте —
# он должен установить его через subprocess:
# subprocess.check_call([sys.executable, "-m", "pip", "install", "fastapi"])
# ============================================
